[phases.setup]
nixPkgs = [
    "nodejs",
    "python3",
    "python3Packages.pip",
    "python3Packages.virtualenv"
]

[phases.install]
cmds = [
    "npm install"
]

[phases.build]
cmds = [
    "npm run build"
]

[start]
cmd = "source venv/bin/activate && npm start" 